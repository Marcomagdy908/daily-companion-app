{
  "name": "daily-companion-functions",
  "description": "Cloud Functions for Daily Companion (رفيق يومي)",
  "scripts": {
    "lint": "eslint .",
    "serve": "firebase emulators:start --only functions",
    "shell": "firebase functions:shell",
    "start": "npm run shell",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log"
  },
  "engines": {
    "node": "20"
  },
  "main": "index.ts",
  "dependencies": {
    "firebase-admin": "^12.6.0",
    "firebase-functions": "^6.2.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "@types/node": "^22.10.0"
  },
  "private": true
}
